# OmsiLaunch Plugin Boundary

This project is reserved for the reusable OMSI plugin/bootstrap implementation.
It must retain the UI-thread callback and fingerprint-gated build adapter, but
must not expose the archived lab operation name or commercial TransitCrew
logic.
