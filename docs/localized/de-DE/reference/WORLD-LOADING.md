> Lokalisierte Übersetzung. Bei technischen Abweichungen gilt die kanonische englische Dokumentation.

# Welt wird geladen

Status: NORMATIV

„NEW_MAP“ ist der zur Laufzeit validierte kanonische Pfad. Karten- und Einstiegspunktidentität
sind semantische Werte; Ein präsentierter Einstiegspunktindex ist ein Diagnose-/Low-Level-Index
außer Kraft setzen, keine dauerhafte Identität.

Startup Handoff v4 kann einen Einstiegspunktselektor und eine kanonische gespeicherte Situation enthalten
Identität zur nativen Startup-Grenze. Der Einstiegspunktselektor erreicht den nativen
„Tform_setpos“-Präsentationsgrenze. Der Adapter kann genau einem Unikat entsprechen
Unicode-Beschriftung der präsentierten Liste, dann delegiert die Auswahl an die vorhandene
„Button1Click“-Fluss. Es greift nie stillschweigend auf einen Index zurück. Die Öffentlichkeit
Die kanonische Identität bleibt fähigkeitsabhängig: Die rohen „global.cfg“-Labels sind es
nicht unbedingt die dargestellten Etiketten und können dupliziert werden, daher sind sie genau
Eine Laufzeitkorrelation ist erforderlich, bevor eine Identität gestartet werden kann.

Die Offline-Erkennung macht jeden „[Einstiegspunkte]“-Datensatz verfügbar als
`<map>#entrypoint:<sha256-of-normalized-record>`. Dies ist ein stabiler Inhalt
Identität statt einer Ordnungszahl oder Bezeichnung, bleibt aber bis dahin nur für die Entdeckung zugänglich
Das Profil schließt seine Zuordnung zur nativen präsentierten Liste. Das validierte
Grundorf-Basisdatensätze „präsentierter Index 1 -> Rohindex 0 -> Nordspitze“.
Bauernhof`; Die VCL-Präsentationstextdarstellung ist immer noch ungelöst.
Korrelation erfordert eine strukturierte Identität vor der Veröffentlichung.

„SAVED_SITUATION“ stellt einen ausgewählten kanonischen „.osn“-Pfad dar und verwendet den
nativer Startform-Situationszweig. Das Profil zeichnet die Evidenzgrenze auf:
„Tform_start+0x3E4“ ist die Steuerung der ausgewählten Situation, „+0x3F8“ ist die geordnete
native Sammlung und Versand ist „Tform_start.LoadSelectedSituation“.
(`0x0064307C`) für `Omsi23004_692EBFBF`. Die aktuelle native Bridge löst das Problem
kanonischer Pfad in dieser Live-Unicode-Sammlung, ruft jedoch niemals diesen Dispatcher auf
direkt: Dadurch wird der formeigene Status umgangen. Es wählt das OMSI-eigene Radio aus
Modus, legt den profilierten Selektor über seinen nativen VMT-Setter fest und ruft den auf
Auswahlsynchronisierungshandler und führt dann „Button1Click“ aus. Das ist
`RUNTIME_VALIDATED`: die installierte Berlin-Spandau-Situation
`situations\\Baustelle Falkenseer Ch..osn` erreichte `gameplay.entered` über
der öffentliche Sitzungspfad. Das native Laden dauerte etwa 54 Sekunden, so die aktuelle
Das Standard-Startzeitlimit beträgt 180 Sekunden. Anrufer können eine genauere Aussage treffen
gebunden, wenn bekannt ist, dass ihre Inhalte schneller geladen werden.

Bevor ein Laufzeitartefakt bereitgestellt wird, löst „PlanSession“ die von deklarierte Karte auf
die ausgewählte „.osn“ mit der aktuellen Installation. Eine fehlende Karte wird abgelehnt
als „OL_E_SITUATION_MAP_NOT_FOUND“; OmsiLaunch lässt den nativen Loader nicht zu
um einen modalen Fehler wegen fehlendem Inhalt darzustellen und den Headless-Startpfad zu blockieren.

„LAST_MAP_STATE“ bezeichnet das native automatische Wiederherstellungsverhalten der letzten Karte. Es ist
nicht die neueste „.osn“, ein Dateisystem-Zeitstempel oder eine Verzeichnisreihenfolge-Heuristik.
„laststn.osn“ und „laststn.osn.owt“ sind nur Beweise, bis der native Zweig verfügbar ist
geschlossen. Die Funktion ist daher für das aktuelle Profil nicht verfügbar.

